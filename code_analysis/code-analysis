#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if ! command -v bun >/dev/null 2>&1; then
  printf '[FAIL] bun is required to run the code-analysis CLI\n' >&2
  exit 127
fi

exec bun run "$SCRIPT_DIR/engine/src/cli.ts" "$@"
